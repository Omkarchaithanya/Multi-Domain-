{
  "source": "en_articles_13945233-use-claude-in-excel-and-powerpoint-with-an-llm-gateway_6.json",
  "domain": "claude",
  "text": "attributes. Run /claude-in-office:update-user-attrs with the per-user keys after the initial setup. Per-user configuration with a bootstrap endpoint If some values vary per user or if per-user values need server-side logic—for example, MCP server lists, skills, inference headers, or short-lived gateway tokens vended from your secrets store—configure a bootstrap endpoint instead. Set bootstrap_url in the manifest to an HTTPS endpoint you host; the add-in calls it with the user's Entra token and applies whatever JSON you return. Run /claude-in-office:bootstrap for the request/response contract and a handler scaffold. Deploy to Microsoft 365 After the wizard generates your manifest: Open the Microsoft 365 Admin Center and go to Settings > Integrated Apps > Upload custom apps . Open the Microsoft 365 Admin Center and go to Settings > Integrated Apps > Upload custom apps . Select “Office Add-in” as the app type, then upload the manifest.xml file. Select “Office Add-in” as the app type, then upload the manifest.xml file. Choose who gets the add-in: If all users share the same configuration, select “Entire organization.” If you wrote per-user attributes in the previous step, assign to Specific users/groups matching exactly who was configured. Anyone else would open the add-in with no configuration. Choose who gets the add-in: If all users share the same configuration, select “Entire organization.” If all users share the same configuration, select “Entire organization.” If you wrote per-user attributes in the previous step, assign to Specific users/groups matching exactly who was configured. Anyone else would open the add-in with no configuration. If you wrote per-user attributes in the previous step, assign to Specific users/groups matching exactly who was configured. Anyone else would open the add-in with no configuration. Accept permissions and finish deployment. Accept permissions and finish deployment. Propagation to users takes up to 24 hours (usually much faster). The add-in appears under Home > Add-ins in Excel, PowerPoint, and Word once it lands. Note: Start with a pilot group to confirm the add-in works, then widen the assignment. You can change assignment later without redeploying. After deploying the add-in, your users can connect by following the steps below. Connection instructions for end users LLM gateway Open Excel, PowerPoint, or Word and launch the Claude add-in. Open Excel, PowerPoint, or Word and launch the Claude add-in. On the sign-in screen, select \"Enterprise gateway.\" On the sign-in screen, select \"Enterprise gateway.\" Enter the Gateway URL and API token your IT team provided.",
  "url": "https://support.claude.com/en/articles/13945233-use-claude-in-excel-and-powerpoint-with-an-llm-gateway"
}