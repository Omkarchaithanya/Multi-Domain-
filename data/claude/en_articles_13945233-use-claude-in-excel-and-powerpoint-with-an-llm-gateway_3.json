{
  "source": "en_articles_13945233-use-claude-in-excel-and-powerpoint-with-an-llm-gateway_3.json",
  "domain": "claude",
  "text": "AI. Domain Domain Required when Required when Purpose Purpose pivot.claude.ai pivot.claude.ai Always Always Add-in host. Serves the task pane UI and proxies analytics, icon search, and telemetry. Add-in host. Serves the task pane UI and proxies analytics, icon search, and telemetry. claude.ai/api/ claude.ai/api/ Always Always Feature-flag evaluation. No sign-in; the add-in only fetches its configuration from here. Feature-flag evaluation. No sign-in; the add-in only fetches its configuration from here. appsforoffice.microsoft.com appsforoffice.microsoft.com Always Always Microsoft Office.js runtime script (required by every Office add-in). Microsoft Office.js runtime script (required by every Office add-in). login.microsoftonline.com login.microsoftonline.com Always Always Microsoft Entra ID sign-in via Nested App Auth. Reads admin-provisioned gateway config and issues tokens for direct-cloud auth. Microsoft Entra ID sign-in via Nested App Auth. Reads admin-provisioned gateway config and issues tokens for direct-cloud auth. o1158394.ingest.us.sentry.io o1158394.ingest.us.sentry.io Optional Optional Crash and error reporting. Blocking this degrades diagnostics only; the add-in still works. Crash and error reporting. Blocking this degrades diagnostics only; the add-in still works. Your LLM gateway URL Your LLM gateway URL If using an LLM gateway If using an LLM gateway Your organization's LLM gateway (LiteLLM, Portkey, Kong, etc.). Inference goes here instead of api.anthropic.com. Your organization's LLM gateway (LiteLLM, Portkey, Kong, etc.). Inference goes here instead of api.anthropic.com. sts.amazonaws.com sts.amazonaws.com If using Bedrock direct If using Bedrock direct AWS STS. Exchanges the Entra ID token for temporary Bedrock credentials. AWS STS. Exchanges the Entra ID token for temporary Bedrock credentials. bedrock-runtime.<region>.amazonaws.com bedrock-runtime.<region>.amazonaws.com If using Bedrock direct If using Bedrock direct Bedrock inference endpoint. Replace <region> with your configured AWS region (for example, us-east-1). Bedrock inference endpoint. Replace <region> with your configured AWS region (for example, us-east-1). accounts.google.com accounts.google.com If using Vertex AI direct If using Vertex AI direct Google OAuth consent screen. Google OAuth consent screen. oauth2.googleapis.com oauth2.googleapis.com If using Vertex AI direct If using Vertex AI direct Google OAuth token exchange and refresh. Google OAuth token exchange and refresh. aiplatform.googleapis.com aiplatform.googleapis.com If using Vertex AI direct If using Vertex AI direct Vertex AI global inference endpoint. Vertex AI global inference endpoint. <region>-aiplatform.googleapis.com <region>-aiplatform.googleapis.com If using Vertex AI direct If using Vertex AI direct Vertex AI regional inference endpoint. Replace <region> with your configured GCP region (for example, us-east5). Vertex AI regional inference endpoint. Replace <region> with your configured GCP region (for example, us-east5). <resource>.services.ai.azure.com <resource>.services.ai.azure.com If using Foundry direct If using Foundry direct Azure AI Foundry inference endpoint. Replace <resource>",
  "url": "https://support.claude.com/en/articles/13945233-use-claude-in-excel-and-powerpoint-with-an-llm-gateway"
}